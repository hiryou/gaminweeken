# Place to host custom apps for the retrodroid

blah blah

## Android dev getting started

Install android SDK if not already. This is on Mac:
```shell
$ brew install --cask android-commandlinetools
# once done, the SDK root is: /opt/homebrew/share/android-commandlinetools
```

Set env vars for this shell:
```shell
$ export ANDROID_HOME=/opt/homebrew/share/android-commandlinetools
$ export ANDROID_SDK_ROOT=/opt/homebrew/share/android-commandlinetools
```

